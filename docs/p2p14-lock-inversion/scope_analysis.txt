P2P-14/15 SCOPE ANALYSIS — which locks are held at each site
Tree: 9f594a99 (branch fix/p2p14-15-cs-headers-lock-order, base origin/main 9e12e649)
Tool: brace-depth scope tracker, strings/char-literals/comments stripped before counting.
      Grep cannot answer 'is this lock held here' and produced a WRONG shape on this finding twice.
      Validated 3/3 against known-truth cases before its results were used (see below).

=== VALIDATION (must pass before any result below is trusted) ===
498: [held: cs_headers] connman->PushMessage(peer, getheaders);
1701: [held: cs_peers] collector->on_peer_disconnected(peer_dna_id);
1708: [held: -] g_node_context.headers_manager->OnPeerDisconnected(peer_id);
  ^ expected: :498 cs_headers HELD | :1701 cs_peers HELD | :1708 NOTHING held
    (:1708 is the closed-scope case that nearly killed a true finding)

=== AFTER THE FIX: no outbound dispatch under cs_vNodes in connman.cpp ===
  (empty = the forward edge is broken at the primary reaper CConnman::DisconnectNodes)

=== reverse-edge sites still under cs_headers in headers_manager.cpp ===
351: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
498: [held: cs_headers] connman->PushMessage(peer, getheaders);
530: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
674: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
2917: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
2987: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
  These 6 remain and are CONFORMING under the ratified order (cs_headers ABOVE
  cs_vNodes/cs_peers, net.h). They are not violations; they are what establishes
  that direction. The cycles close because the DISCONNECT path took cs_headers
  while holding the other two — that is the site this branch fixes.
