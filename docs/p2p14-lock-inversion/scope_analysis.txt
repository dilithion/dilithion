P2P-14/15 SCOPE ANALYSIS — which locks are held at each site
Tree: d63df3a6  base origin/main 105f3d8b

############################################################################
# ⛔ TWO LIMITATIONS. BOTH BIT THIS MISSION. READ BEFORE USING ANY LINE BELOW.
#
# (1) PER-FILE AND LEXICAL — it cannot establish acyclicity.
# It answers 'is this lock textually in scope at this line, IN THIS FILE'.
# The real question is 'can lock B be acquired while A is held, ANYWHERE'.
# THREE passes declared this cycle closed and were wrong:
#   pass 1  my analysis   -> missed the accept path   (connman.cpp:447)
#   pass 2  after that fix-> missed the eviction path (peers.cpp:963)
#   pass 3  review seats  -> caught both, by CALL GRAPH, not by this tool
# The trap is in this file's own validation block: 'peers.cpp:1708 [held: -]'
# is the per-file view of that function's BODY. Its CALLERS held locks.
#
# (2) IT DOES NOT MODEL std::unique_lock::unlock(). ⚠️ FALSE POSITIVES.
# It tracks guard DECLARATIONS and brace scope only. After an explicit
# lock.unlock() it still reports the lock as held for the rest of the scope.
# Live example in this very tree — the tool says:
#     1120: [held: cs_peers] connman->DispatchPeerDisconnected(peer_to_evict)
# and that is WRONG: peers.cpp:1118 is 'lock.unlock();'. cs_peers is NOT held
# at :1120. Errs toward reporting a lock held, which is the safe direction for
# hunting inversions and the WRONG direction for certifying one is closed.
############################################################################

=== VALIDATION (tool sanity within one file — NOT an acyclicity claim) ===
498: [held: cs_headers] connman->PushMessage(peer, getheaders);
1701: [held: -] {

=== connman.cpp: eviction/dispatch/RemoveNode under cs_vNodes ===
1332: [held: cs_vNodes] // EvictPeersIfNeeded() here: it would acquire CPeerManager's
  (empty of CALLS; comment lines quoting these names may still appear)

=== peers.cpp: the eviction leg — READ WITH LIMITATION (2) IN MIND ===
  peers.cpp:1118  lock.unlock();      <- cs_peers RELEASED here
  peers.cpp:1120  DispatchPeerDisconnected(peer_to_evict)   <- NOT under cs_peers
  The tool cannot see the unlock and will label :1120 as held. Verified by
  reading the source, not by the tool.

=== reverse-edge sites under cs_headers (CONFORMING to the net.h order) ===
351: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
498: [held: cs_headers] connman->PushMessage(peer, getheaders);
530: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
674: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
2937: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
3007: [held: cs_headers] g_node_context.peer_manager->Misbehaving(peer, 20, MisbehaviorType::INVALID_BLOCK_HEADER);
